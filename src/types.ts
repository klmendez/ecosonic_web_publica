export type Environmental={puntoId:number;puntoNombre:string;nombre_tecnico?:string;direccion_completa?:string;calle?:string;barrio?:string;lugar_cercano?:string;latitude:number;longitude:number;tipo_entorno:string;dist_to_green:number;n_evaluaciones:number;indice_naturaleza_integrado:number;indice_presion_trafico:number;indice_prioridad_intervencion:number;recomendacion:string;prioridad_intervencion:string;DAP_antes_regla_005:number|null};
export type Stat={label:string;value:number|null};
export type Evidence={visual:Stat[];acoustic:Stat[];visual_scope:string;acoustic_scope:string;acoustic_segments:number;observations:number};
export type MapData={classes:string[];periods:{id:string;label:string}[];maps:{class_name:string;period:string;period_label:string;href:string}[];class_means:Record<string,number>;period_means?:Record<string,Record<string,number>>;error?:string};
export type Prediction={point:number;period:string;minute:number;expected_dap:number;probability_pay:number;amount_if_pay:number;interval80:[number,number];interval90:[number,number];confidence:string;traffic:number;nature:number;balance:number;profile:{edad:number;ingresos:number;personas_hogar:number};scope:string};
